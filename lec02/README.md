Послідовність виконання завдання

1. Треба створити репозиторій на GitHub, в якому ви писатимете код для домашніх завдань. Створити окрему робочу гілку для цього завдання. Саме завдання виконувати в папці lec02.

2. Написати джобу, яка витягує всі дані продажів з API та зберігає їх у raw-директорію:

➡️ як шаблон для джоби використати код у папці: [link]

➡️ API доступна за адресою: https://fake-api-vycpfa6oca-uc.a.run.app/

  1. Секретний токен до API: 2b8d97ce57d401abd89f45b0079d8790edd940e6
  2. Приклад, як діставати дані з API: [link]
  3. Секретний токен в жодному разі не можна зберігати в коді, він обовʼязково має зберігатися у змінній середовища з назвою AUTH_TOKEN
  4. У файлі main.py має бути реалізований WEB-сервер за допомогою фреймворку Flask, який працює локально і приймає команди у вигляді POST-запитів на порт 8081
  5. Сервер має приймати JSON-обʼєкт такого вигляду: [link - натисніть кнопку Відправити для отримання результату виклику API]
  6. Шлях зберігання повинен мати вигляд: /path/to/my_dir/raw/sales/2022-08-09. Цей шлях треба подавати у вигляді параметра raw_dir для джоби
  7. Джоба обовʼязково має бути ідемпотентною. Для цього джоба сама має очищати вміст директорії raw_dir перед записом нових файлів.
  8. Кожну «сторінку» з API можна зберігати в окремому файлі або всі сторінки — в одному великому JSON. Файл повинен мати імʼя sales_2022-08-09.json або sales_2022-08-09_1.json, sales_2022-08-09_2.json тощо, якщо файлів декілька.

3. Написати джобу, яка JSON-файли з raw-директорії записує в stg-директорію в форматі Avro. Всі умови залишаються з попередньої джоби, окрім:

  1. Джоба має запускатися POST-запитом на localhost:8082 з 2 параметрами в JSON: raw_dir та stg_dir
  2. stg_dir повинна мати вигляд: /path/to/my_dir/stg/sales/2022-08-09
  3. raw_dir повинна мати такий самий вигляд, як і для першої джоби
  4. Як шаблон для коду можна модифікувати код з першої джоби
  5. Потрібно використати бібліотеку fastavro

4. Самостійно перевірити, чи обидві джоби працюють коректно, можна таким чином:

  1. Запустити Flask-сервер першої джоби в одному терміналі, а другої джоби — в іншому. Зверніть увагу, що сервери обох джоб мають працювати на різних портах.
  2. Запустити скрипт для перевірки: [link]
  3. Якщо скрипт не впав, а відпрацював коректно, це свідчить, що код не має синтаксичних помилок
  4. Перевірити правильність обох джоб можна, переглянувши файли в raw та stg директоріях
  5. Результат повинен мати такий вигляд, якщо ви зберігали всі дані в одному файлі (тут коренева директорія називається file_storage, ви можете обрати іншу назву, внутрішні директорії та імена файлів повинні мати такий самий вигляд):

image.jpeg

або так, якщо ви зберігали кожну «сторінку» в окремому файлі:




 image.jpeg

5. Створити пул-реквест (надалі — PR), лектора призначити ревʼювером PR. Лінку на PR прикріпити в LMS до цього уроку.

6. Вітаю, ви побудували перший в житті data pipeline!



Додаткове завдання (на додаткові бали)

1. Виконайте локальний деплой за допомогою docker-compose pull-based сервісу, що вилучає поїздки таксі та записує їх в базу даних SQLite [link на опис сервісу для докеру]
2. Напишіть python сервіс, що забирає дані з таблиці rides, обчислює загальну дистанцію, що проїхав водій за весь період часу, та записує результат в таблицю driver_statistics. Приклад коду сервісу знаходиться за посиланням: [link] 
3. Додайте ваш сервіс до docker-compose файлу та створіть Dockerfile з метою ініціалізації образу (image) сервісу. Як приклад для Dockerfile, можна скористатися вже написаним файлом для серверу, що емулює API поїздок -  [link]
4. (Виконавши цей пункт x2 балів або звільнення від Завдання з теми Docker): 
  1. зареєструйтеся на heroku.com - хмарному сервісі для хостингу ПЗ
  2. завантажте heroku CLI
  3. заповніть файл heroku.yaml
  4. виконайте деплой сервісів на heroku

👏 Вітаю з першим деплоєм та написаним pull-based сервісом!

Нотатки

1. Стиль коду має обовʼязково відповідати стандарту PEP8.
2. Код має бути покритий тестами. В реальному проєкті PR без тестів не приймають. Але оскільки в усіх слухачів різний досвід, то тести можна упустити або дописати пізніше (як виняток).
3. Дуже бажано користуватися type hints.
4. Якщо дуже важко, то замість обох джоб можна написати одну на вибір. Краще написати одну джобу, але правильно, з type hints та тестами, і щоб вона працювала.
5. Це завдання можна також виконати трішки по-іншому, використавши у файлі main.py інтерфейс командного рядка (CLI). Для цього, наприклад, можна використати стандартний модуль argparse (офіційний туторіал можна знайти тут). Крім того, такий спосіб рекомендують студентам, які не мають досвіду з web та вебсервером Flask. На кількість балів це не вплине.
6. Для більш досвідчених людей можна цей пайплайн модифікувати й задеплоїти у клауд на вибір (GCP, AWS, Azure, Heroku, etc.). Це можна також зробити самостійно пізніше, навіть після курсу. У цьому завданні перевірятиметься лише код.

Якщо Ви вирішите модифікувати й задеплоїти результат у клауд, будьте уважні: з вас може стягуватись оплата за ресурси!
